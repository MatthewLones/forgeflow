# Use hoisted node_modules layout (flat, like npm).
# Required for Electron: pnpm's default symlink layout causes the npm 'electron'
# package to shadow Electron's built-in require("electron") module at runtime.
node-linker=hoisted
