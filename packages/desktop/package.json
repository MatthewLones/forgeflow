{
  "name": "@forgeflow/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "build": "electron-vite build --config electron-vite.config.ts",
    "dev": "electron-vite build --config electron-vite.config.ts && electron .",
    "start": "electron ."
  },
  "dependencies": {
    "@forgeflow/server": "workspace:*"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-vite": "^5.0.0"
  }
}
